/*
*
* 배열에 데이터를 넣고 배열의 모든 값의 합을 구하시오
*/
public class Ex13 {

  public static void main(String[] args) {
  int[] array = {10,20,30,40,50};
  int sum;
  
  //배열의 모든 요소의 합을 구하기
  System.out.println("sum = " + sum);
  
  for (int index=0; index<array.length(); ++index) {
  sum += array[index]; //sum = sum + array[index];
  }
  System.out.println("sum=" + sum)
  
  }
  }
